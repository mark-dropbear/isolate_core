name: isolate_core
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  base32_codec: ^1.0.0
  file: ^7.0.1
  flutter:
    sdk: flutter
  go_router: ^17.2.2
  logging: ^1.3.0
  rdf_dart:
    git:
      url: https://github.com/mark-dropbear/rdf_dart.git
  url_launcher: ^6.3.2
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  meta: 1.17.0
